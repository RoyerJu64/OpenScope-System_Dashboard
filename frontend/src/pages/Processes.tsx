/**
 * Page Processus — la table complète (tri, recherche, kill, renice)
 * arrive avec l'issue #28 ; le backend (list_processes, kill_process,
 * set_priority) est déjà en place.
 */
export function Processes() {
  return (
    <main class="main">
      <h1 class="page-title">Processus</h1>
      <div class="card placeholder">
        Table des processus en cours de construction (issue #28) — le
        backend est prêt.
      </div>
    </main>
  );
}
