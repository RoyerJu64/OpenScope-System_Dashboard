/**
 * Page Paramètres — les intervalles de rafraîchissement par collecteur
 * arrivent avec l'issue #29 ; les commands get/set_collector_interval
 * sont déjà en place.
 */
export function Settings() {
  return (
    <main class="main">
      <h1 class="page-title">Paramètres</h1>
      <div class="card placeholder">
        Réglages des intervalles de rafraîchissement en cours de
        construction (issue #29) — le backend est prêt.
      </div>
    </main>
  );
}
